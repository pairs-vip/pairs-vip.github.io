{"title":"JVM 虚拟机","date":"2026-08-13T10:25:56.000Z","date_formatted":{"ll":"2026年8月13日","L":"2026/08/13","MM-DD":"08-13"},"author":"pairs","link":"2026/08/13/JVM虚拟机","tags":["GC","JVM"],"categories":["dev"],"updated":"2026-09-14T06:56:52.686Z","content":"<h1 id=\"jvm-虚拟机\">JVM 虚拟机<a title=\"#jvm-虚拟机\" href=\"#jvm-虚拟机\"></a></h1>\n<hr>\n<h2 id=\"一、jvm-架构概览\">一、JVM 架构概览<a title=\"#一、jvm-架构概览\" href=\"#一、jvm-架构概览\"></a></h2>\n<p>JVM虚拟机：类装载子系统，运行时数据区（内存模型），字节码执行引擎</p>\n<p><strong>运行时数据区（内存模型）：</strong> 堆，栈（线程），本地方法栈，方法区（元空间），程序计数器</p>\n<p><strong>栈（线程）：</strong> 栈帧</p>\n<p><strong>栈帧：</strong> 局部变量，操作数栈，动态链接，方法出口</p>\n<p>栈里面会存对象在堆中的内存地址，也就是引用</p>\n<p><strong>方法区（元空间）：</strong> 常量，静态变量，类信息 —— 静态变量User对象存的也是通过指针指向堆中的具体对象</p>\n<p><img src=\"https://cdn.jsdelivr.net/gh/pairs-vip/source-pairs-vip.github.io@main/static_resource/jvm/%E6%9E%B6%E6%9E%84%E6%A6%82%E8%A7%88.png\" alt=\"JVM架构概览\" loading=\"lazy\" class=\"φbp\"></p>\n<hr>\n<h2 id=\"二、gc-内存回收流程\">二、GC 内存回收流程<a title=\"#二、gc-内存回收流程\" href=\"#二、gc-内存回收流程\"></a></h2>\n<p>Eden内存满了之后会触发 minor GC</p>\n<p><strong>minor GC垃圾回收机制：</strong></p>\n<p>（待补充）</p>\n<p>老年代内存也满了会触发full GC</p>\n<p><strong>full GC 全量垃圾回收：</strong></p>\n<p>（待补充）</p>\n<p>full GC之后老年代还是满的触发 OOM</p>\n<hr>\n<h2 id=\"三、gc-安全点\">三、GC 安全点<a title=\"#三、gc-安全点\" href=\"#三、gc-安全点\"></a></h2>\n<p>GC垃圾收集只会在<strong>安全点</strong>才能进行，安全点是JVM中程序执行的某些特定位置，JVM只能在安全点暂停执行，然后进行GC等操作，安全点的设定确保了当线程暂停时，程序的状态是可知和一致的。</p>\n<p><strong>安全点的触发条件：</strong></p>\n<ol>\n<li><strong>方法调用：</strong> 每次方法调用都是一个潜在的安全点</li>\n<li><strong>循环回跳：</strong> 长时间循环中间会插入安全点检查</li>\n<li><strong>异常处理：</strong> 处理异常时，也会就检查是否到达安全点</li>\n</ol>\n<hr>\n<h2 id=\"四、类的生命周期\">四、类的生命周期<a title=\"#四、类的生命周期\" href=\"#四、类的生命周期\"></a></h2>\n<p><strong>类的生命周期：</strong> 加载，连接，初始化，使用，卸载</p>\n<p><strong>连接：</strong> 验证，准备，解析</p>\n<ul>\n<li><strong>加载：</strong> 就是class磁盘到JVM内存的过程</li>\n<li><strong>验证：</strong> 验证class是否正确，格式等</li>\n<li><strong>准备：</strong> 静态变量初始化，静态对象等初始化赋值</li>\n<li><strong>解析：</strong> 字符引用解析成直接引用 —— 调用方法时，只知道方法的字符，将字符引用解析为直接引用</li>\n<li><strong>初始化：</strong> 不一定执行，用到了这个类的时候初始化，new 或者要用这个类的方法</li>\n<li><strong>使用：</strong></li>\n<li><strong>卸载：</strong> GC垃圾回收</li>\n</ul>\n<hr>\n<h2 id=\"五、垃圾收集算法\">五、垃圾收集算法<a title=\"#五、垃圾收集算法\" href=\"#五、垃圾收集算法\"></a></h2>\n<p>垃圾收集算法：常用的为以下三种</p>\n<ul>\n<li><strong>标记-清除算法</strong></li>\n<li><strong>标记-复制算法</strong> —— 将内存分为两半，类似S1和S2区</li>\n<li><strong>标记-整理算法</strong> —— 将么用的内存清空，正在使用的进行整理</li>\n</ul>\n<hr>\n<h2 id=\"六、三色标记算法\">六、三色标记算法<a title=\"#六、三色标记算法\" href=\"#六、三色标记算法\"></a></h2>\n<ul>\n<li>用于垃圾回收器升级，将STW变为并发标记。STW就是在标记垃圾的时候，必须暂停程序，而使用并发标记，就是程序一边运行，一边标记垃圾。</li>\n<li>避免重复扫描对象，提升标记阶段的效率</li>\n</ul>\n<h3 id=\"什么是三色标记\">什么是三色标记<a title=\"#什么是三色标记\" href=\"#什么是三色标记\"></a></h3>\n<p>三色标记就是根据可达性分析，从GC Roots开始进行遍历访问，在遍历对象过程中，按’是否检查过’这个条件，将对象标记为三种颜色：</p>\n<ul>\n<li><strong>白色：</strong> 该对象没有被标记过。（对象垃圾）</li>\n<li><strong>灰色：</strong> 该对象已经被标记过了，但该对象下的属性么有全被标记完。（GC需要从此对象中去寻找垃圾）</li>\n<li><strong>黑色：</strong> 该对象已经被标记过了，且该对象下的属性也全部被标记过了。（程序所需要的对象）</li>\n</ul>\n<h3 id=\"对象漏标问题\">对象漏标问题<a title=\"#对象漏标问题\" href=\"#对象漏标问题\"></a></h3>\n<p>关于对象漏标问题，三色标记已经标记了对象A为垃圾对象，但是在执行GC的时候，对象A突然被引用，现在不是垃圾对象了，此时就会出现对象漏标问题（需要的对象被回收），导致系统出现问题。CMS和G1垃圾回收器在使用三色标记算法时，都采取了一些措施来应对这些问题，CMS对增加引用的环节进行处理（Increment Update），G1对删除引用环节进行处理（SATB）。</p>\n<hr>\n<h2 id=\"七、cms-收集器\">七、CMS 收集器<a title=\"#七、cms-收集器\" href=\"#七、cms-收集器\"></a></h2>\n<p>CMS（Concurrent Mark Sweep）收集器的垃圾收集分为4部分：</p>\n<ol>\n<li><strong>初始标记（CMS initial mark）：</strong> 单线程运行，需要Stop The World，标记GC Roots能直达的对象。</li>\n<li><strong>并发标记（CMS concurrent mark）：</strong> 无停顿，和用户线程同事运行，从GC roots直达对象开始遍历整个对象图。</li>\n<li><strong>重新标记（CMS remark）：</strong> 多线程运行，需要STW，标记并发标记阶段产生对象。</li>\n<li><strong>并发清除（CMS concurrent sweep）：</strong> 无停顿，和用户线程同时运行，清除掉标记阶段标记的死亡对象。</li>\n</ol>\n<hr>\n<h2 id=\"八、g1-收集器\">八、G1 收集器<a title=\"#八、g1-收集器\" href=\"#八、g1-收集器\"></a></h2>\n<p>G1（Garbage First）收集器的垃圾收集：</p>\n<p>G1把连续的Java堆划分为多个大小相等的独立区域（Region），每一个Region都可以根据需要，扮演新生代的Eden空间，Survivor空间，或者老年代空间。收集器能够堆扮演不同角色的Region采用不同的策略去处理。</p>\n<p><strong>更精细的控制，可预测的停顿时间，内存碎片的控制，优先级处理</strong></p>\n<p>G1收集器的运行过程大致可以划分为以下四个步骤：</p>\n<ol>\n<li><strong>初始标记（initial mark）：</strong> 标记了从GC Roots开始直接关联可达的对象，STW执行</li>\n<li><strong>并发标记（concurrent maring）：</strong> 和用户线程并发执行，从GC Roots开始，对堆中的对象进行可达性分析，递归扫描整个堆里的对象图，找出要回收的对象。</li>\n<li><strong>最终标记（Remark）：</strong> STW，标记在并发标记过程中产生的垃圾。=重新标记（标记的范围更小）</li>\n<li><strong>筛选回收（Live Data Counting And Evacuation），</strong> 制定回收计划，选择多个Region构成回收集，把回收集中Region的存货对象复制到空的Region中，再清理掉整个旧Region的全部空间，需要STW。</li>\n</ol>\n<h3 id=\"有了cms，为什么还有引用g1\">有了CMS，为什么还有引用G1<a title=\"#有了cms，为什么还有引用g1\" href=\"#有了cms，为什么还有引用g1\"></a></h3>\n<p>主要还是为了应对不同的服务器资源</p>\n<p><strong>优点：</strong> CMS最主要的有点在名字上已经体现了出来------并发收集、低停顿</p>\n<p><strong>缺点：</strong></p>\n<ul>\n<li>Mark Sweep算法会导致内存碎片比较多</li>\n<li>CMS的并发能力比较依赖CPU，并发回收垃圾收集线程可能会抢占用户线程，导致用户程序性能下降</li>\n<li>并发清除阶段，用户线程依然运行，会产生所谓的&quot;浮动垃圾&quot;，需要等到下一次垃圾回收处理。</li>\n</ul>\n<p>G1主要解决了内存碎片过多的问题。</p>\n<hr>\n<h2 id=\"九、逃逸分析\">九、逃逸分析<a title=\"#九、逃逸分析\" href=\"#九、逃逸分析\"></a></h2>\n<p>一个对象创建出来不一定在堆里面，由这个对象逃逸分析以及对象大小来决定。</p>\n<p><strong>逃逸分析的好处：</strong></p>\n<ul>\n<li>\n<p><strong>栈上分配</strong></p>\n<p>如果确定这个对象不会逃逸到线程外，那么就可以考虑将这个对象在栈上分配，对象占用的内存随着栈帧出栈而销毁，减轻垃圾收集的压力。</p>\n</li>\n<li>\n<p><strong>同步消除</strong></p>\n<p>如果当前对象只在当前方法使用，并没有逃逸，无法被其他线程使用，对这个变量实施的同步措施也就可以安全的消除掉。</p>\n</li>\n<li>\n<p><strong>标量替换</strong></p>\n<p>如果一个数据是基本数据类型，不可拆分，他就被称为标量。确认对象未逃逸的时候，就是把对象拆成一个个的成员变量，直接在栈上面分配内存存储，并且可以以内存碎片的方式存储，充分利用内存碎片，改善内存碎片化。</p>\n</li>\n</ul>\n<hr>\n<h2 id=\"十、类加载器\">十、类加载器<a title=\"#十、类加载器\" href=\"#十、类加载器\"></a></h2>\n<h3 id=\"类加载流程-——-jdk-9-前的双亲委派模式\">类加载流程 —— JDK 9 前的双亲委派模式<a title=\"#类加载流程-——-jdk-9-前的双亲委派模式\" href=\"#类加载流程-——-jdk-9-前的双亲委派模式\"></a></h3>\n<p>用户自定义了一个 <code>UserClassLoader.load(&quot;&quot;)</code> 方法</p>\n<p>首先交由用户自定义加载器 User ClassLoader → 交给父加载器 → 应用程序加载器 App ClassLoader → 扩展类加载器 Ext ClassLoader → 启动类加载器 BootStrap ClassLoader</p>\n<p>从父加载器开始往下加载并行加载，如果所有的加载器都没有就会报 Class Not Found</p>\n<p>只有当父加载器反馈自己无法完成这个加载请求时，子加载器才会尝试自己去完成加载</p>\n<p><strong>作用：</strong> 唯一，安全</p>\n<hr>\n<h2 id=\"十一、内存泄漏\">十一、内存泄漏<a title=\"#十一、内存泄漏\" href=\"#十一、内存泄漏\"></a></h2>\n<p><strong>内存泄漏原因：</strong></p>\n<ol>\n<li><strong>静态集合类</strong> —— 静态集合生命周期和JVM一致，所以静态集合引用的对象无法被gc</li>\n<li><strong>单例模式</strong> —— 单例对党在初始化之后会以静态变量的方法在JVM的整个生命周期存在，如果单例对象持有外部引用，那么这个外部对象将不能被gc</li>\n<li><strong>数据库连接，IO，Socket等连接</strong> —— 使用完毕应该手动关闭，不手动关闭无法被gc</li>\n<li><strong>变量的不合理的作用域</strong> —— 变量定义的作用哦关于大于其使用范围，可能内存泄漏；或不再使用对象没有及时将独享设置为null，可能导致内存泄漏</li>\n<li><strong>hash值发生变化：</strong> 对象Hash值改变，使用HashMap和HashSet时候，由于对象修改之后的Hash值和存储进容器时Hash值不同，所以无法找到存入的对象，自然也无法单独删除，所以会造成内存泄漏</li>\n<li><strong>ThreadLocal使用不当</strong> —— 线程池+强引用value无法被gc回收，导致内存泄漏</li>\n</ol>\n<hr>\n<h2 id=\"十二、gc-roots\">十二、GC Roots<a title=\"#十二、gc-roots\" href=\"#十二、gc-roots\"></a></h2>\n<p><strong>GC ROOTS都有哪些：</strong> 线程池变量、静态变量、常量池、JNI指针</p>\n<ul>\n<li>虚拟机栈（栈帧中的本地变量表）中引用的对象\n<ul>\n<li>比如：各个线程被调用的方法中使用到的参数、局部变量等（局部变量表）</li>\n</ul>\n</li>\n<li>方法区中类静态属性引用的对象\n<ul>\n<li>比如：Java类的引用类型静态变量</li>\n</ul>\n</li>\n<li>方法区中常量引用的对象\n<ul>\n<li>比如：字符串常量池（String Table）里的引用</li>\n</ul>\n</li>\n<li>所有被同步锁synchronized持有的对象</li>\n</ul>\n<hr>\n<h2 id=\"十三、jvm-监控和故障处理工具\">十三、JVM 监控和故障处理工具<a title=\"#十三、jvm-监控和故障处理工具\" href=\"#十三、jvm-监控和故障处理工具\"></a></h2>\n<h3 id=\"jconsole\">JConsole<a title=\"#jconsole\" href=\"#jconsole\"></a></h3>\n<p><strong>这个工具排查死锁非常方便</strong>，主要用来排查死锁</p>\n<p>Jconsole是一个内置Java性能分析器，通过监控信息，<strong>可以清楚的了解到当前程序是否运行正常，如内存泄漏，死锁，类加载异常等。</strong></p>\n<blockquote>\n<p>备注：Jconsole管理内存相当于可视化的jstat命令</p>\n</blockquote>\n<p><strong>如何启动：</strong> 直接在windows环境下，win+r打开运行输入 <code>JConsole</code> 回车</p>\n<p><strong>可以选择本地和远程，如果需要连接远程，就需要远程java程序在启动命令上添加启动参数：</strong></p>\n<figure class=\"highlight bash\"><table><tr><td class=\"gutter\"><pre><span class=\"line\">1</span><br><span class=\"line\">2</span><br><span class=\"line\">3</span><br><span class=\"line\">4</span><br><span class=\"line\">5</span><br><span class=\"line\">6</span><br></pre></td><td class=\"code\"><pre><span class=\"line\">java -jar xxx.jar</span><br><span class=\"line\">  -Dcom.sun.management.jmxremote                                        <span class=\"comment\"># 远程开启开关</span></span><br><span class=\"line\">  -Dcom.sun.management.jmxremote.port=2808                              <span class=\"comment\"># jmx远程调用端口</span></span><br><span class=\"line\">  -Dcom.sun.management.jmxremote.authenticate=<span class=\"literal\">false</span>                     <span class=\"comment\"># 不开启验证</span></span><br><span class=\"line\">  -Dcom.sun.management.jmxremote.ssl=<span class=\"literal\">false</span>                              <span class=\"comment\"># 不为ssl连接</span></span><br><span class=\"line\">  -Djava.rmi.server.hostname=34.126.141.21                              <span class=\"comment\"># 服务器所在ip或者域名</span></span><br></pre></td></tr></table></figure>\n<h3 id=\"visualvm（jvisualvm）\">VisualVM（jvisualvm）<a title=\"#visualvm（jvisualvm）\" href=\"#visualvm（jvisualvm）\"></a></h3>\n<p><strong>主要分析海量数据（分析dump文件）</strong></p>\n<p>VisualVM是一款免费的，继承了多个JDK命令行工具的可视化工具，他能提供强大的分析能力，对Java应用程序做性能分析和调优。功能包括成成和分析海量数据，根据内存泄漏，监控垃圾回收器，执行内存和CPU分析。</p>\n<p><strong>如何启动：</strong> 直接在windows环境下，win+r打开运行输入 <code>jvisualvm</code> 回车，可能缺失Visual GC插件，可以自行下载</p>\n<p>当Java 程序因为内存溢出（OutOfMemoryError）而崩溃时，这个参数会让 JVM 自动生成一个 <code>.hprof</code> 格式的堆转储文件（也就是 dump 文件）。这个文件就是 JVM 内存在崩溃瞬间的一张&quot;快照&quot;，可以直接用 VisualVM、MAT 等工具打开进行分析，通常两个参数搭配使用，</p>\n<figure class=\"highlight bash\"><table><tr><td class=\"gutter\"><pre><span class=\"line\">1</span><br></pre></td><td class=\"code\"><pre><span class=\"line\">java -XX:+HeapDumpOnOutOfMemoryError -XX:HeapDumpPath=/path/to/your/dumps/ -jar your-application.jar</span><br></pre></td></tr></table></figure>\n<ul>\n<li><code>-XX:+HeapDumpOnOutOfMemoryError</code>：核心开关，开启启动dump功能</li>\n<li><code>-XX:HeapDumpPath=&lt;路径&gt;</code>：（可选）指定dump文件的保存位置和文件名。如果不指定，文件就会默认生成在JVM的启动目录下，文件名类似 <code>java_pid&lt;进程ID&gt;.hprof</code></li>\n</ul>\n<h3 id=\"jps\">jps<a title=\"#jps\" href=\"#jps\"></a></h3>\n<p>查看java进程</p>\n<h3 id=\"jstat\">jstat<a title=\"#jstat\" href=\"#jstat\"></a></h3>\n<p>jstat适用于监视虚拟机各种运行状态信息的命令行工具，可以显示本地或者远程虚拟机进程中的类装载，内存，垃圾收集，JIT编译等运行数据；常用形式：</p>\n<figure class=\"highlight bash\"><table><tr><td class=\"gutter\"><pre><span class=\"line\">1</span><br><span class=\"line\">2</span><br><span class=\"line\">3</span><br></pre></td><td class=\"code\"><pre><span class=\"line\">jstat [-命令选项] [vmid] [间隔时间/毫秒] [查询次数]</span><br><span class=\"line\"><span class=\"comment\"># 如：</span></span><br><span class=\"line\">jstat -gc 13613 100 8</span><br></pre></td></tr></table></figure>\n<p><strong>常用参数：</strong></p>\n<p><strong>-class（类加载器）：</strong></p>\n<div class=\"φbq\"><div class=\"φbs\"><table><thead>\n<tr>\n<th>字段名称</th>\n<th>表示含义</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td>Loaded</td>\n<td>加载class类的数量</td>\n</tr>\n<tr>\n<td>Bytes</td>\n<td>所占内存空间</td>\n</tr>\n<tr>\n<td>Unloaded</td>\n<td>未加载类的数量</td>\n</tr>\n<tr>\n<td>Bytes</td>\n<td>未加载类内存空间</td>\n</tr>\n<tr>\n<td>Time</td>\n<td>消耗时间</td>\n</tr>\n</tbody>\n</table></div></div><p><strong>-compiler（JIT）</strong></p>\n<p><strong>-gc（GC堆状态）：</strong></p>\n<div class=\"φbq\"><div class=\"φbs\"><table><thead>\n<tr>\n<th>字段名称</th>\n<th>表示含义</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td>S0C</td>\n<td>幸存区1容量（单位字节）</td>\n</tr>\n<tr>\n<td>S1C</td>\n<td>幸存区2容量（单位字节）</td>\n</tr>\n<tr>\n<td>S0U</td>\n<td>幸存区1使用容量（单位字节）</td>\n</tr>\n<tr>\n<td>S1U</td>\n<td>幸存区2使用容量（单位字节）</td>\n</tr>\n<tr>\n<td>EC</td>\n<td>伊甸园区容量（单位字节）</td>\n</tr>\n<tr>\n<td>EU</td>\n<td>伊甸园区使用容量（单位字节）</td>\n</tr>\n<tr>\n<td>OC</td>\n<td>老年区容量（单位字节）</td>\n</tr>\n<tr>\n<td>OU</td>\n<td>老年区使用容量（单位字节）</td>\n</tr>\n<tr>\n<td>MC</td>\n<td>方法去容量（单位字节）</td>\n</tr>\n<tr>\n<td>MU</td>\n<td>方法去使用容量（单位字节）</td>\n</tr>\n<tr>\n<td>CCSC</td>\n<td>压缩类空间容量（单位字节）</td>\n</tr>\n<tr>\n<td>CCSU</td>\n<td>压缩类空间使用容量（单位字节）</td>\n</tr>\n<tr>\n<td>YGC</td>\n<td>年轻代垃圾回收次数</td>\n</tr>\n<tr>\n<td>YGCT</td>\n<td>年轻代垃圾回收消耗时间（单位秒）</td>\n</tr>\n<tr>\n<td>FGC</td>\n<td>老年代垃圾回收次数</td>\n</tr>\n<tr>\n<td>FGCT</td>\n<td>老年代垃圾回收消耗时间（单位秒）</td>\n</tr>\n<tr>\n<td>GCT</td>\n<td>垃圾回收器总消耗时间（单位秒）</td>\n</tr>\n</tbody>\n</table></div></div><p>等等</p>\n<h3 id=\"jmap\">jmap<a title=\"#jmap\" href=\"#jmap\"></a></h3>\n<p>jmap用于生成堆转储快照（一般称为heapdump或dump文件）。map的租用并不仅仅是为了获取dump文件，他还可以查询finalize执行队列，java堆和永久代的详细信息，如空间使用率，当前用的是那种收集器等。</p>\n<ul>\n<li><code>heap</code>：显示Java堆详细信息</li>\n<li><code>histo</code>：显示堆中对象的统计信息</li>\n<li><code>permstat</code>：Java堆内存的永久保存区域的类加载器的统计信息</li>\n<li><code>finalizerinfo</code>：显示在F-Queue队列等待Finalize线程执行fializer方法的对象</li>\n<li><code>dump</code>：生成堆转储快照</li>\n</ul>\n<figure class=\"highlight bash\"><table><tr><td class=\"gutter\"><pre><span class=\"line\">1</span><br><span class=\"line\">2</span><br></pre></td><td class=\"code\"><pre><span class=\"line\"><span class=\"comment\"># 生成当前线程PIDjava程序的dump文件</span></span><br><span class=\"line\">jmap -dump:file=d:test.hprof 58390</span><br></pre></td></tr></table></figure>\n<h3 id=\"jhat\">jhat<a title=\"#jhat\" href=\"#jhat\"></a></h3>\n<p>用来分析dump文件</p>\n<p>执行命令 <code>jhat dump 文件名</code> 后，屏幕显示 “server is ready” 提示后，在浏览器输入 <code>http://localhost:7000/</code> 就可以访问dump文件详情信息。</p>\n<h3 id=\"jstack\">jstack<a title=\"#jstack\" href=\"#jstack\"></a></h3>\n<p>用来分析死锁</p>\n<figure class=\"highlight bash\"><table><tr><td class=\"gutter\"><pre><span class=\"line\">1</span><br></pre></td><td class=\"code\"><pre><span class=\"line\">jstack [vmid]</span><br></pre></td></tr></table></figure>\n<h3 id=\"arthas\">Arthas<a title=\"#arthas\" href=\"#arthas\"></a></h3>\n<p><strong>arthas阿里开源诊断工具</strong></p>\n<hr>\n<h2 id=\"十四、jvm-常见参数配置\">十四、JVM 常见参数配置<a title=\"#十四、jvm-常见参数配置\" href=\"#十四、jvm-常见参数配置\"></a></h2>\n<h3 id=\"堆配置\">堆配置<a title=\"#堆配置\" href=\"#堆配置\"></a></h3>\n<ul>\n<li><code>-Xms</code>：初始堆大小</li>\n<li><code>-Xmx</code>：最大堆大小</li>\n<li><code>-XX:NewSize=n</code>：设置年轻代大小</li>\n<li><code>-XX:NewRatio=n</code>：设置年轻代和老年代的比值。如：为3表示年轻代和老年代的比值为1：3，年轻代占整个年轻代老年代和的1/4</li>\n<li><code>-XX:SurvivorRatio=n</code>：年轻代中Eden区和两个Survivor区的比值。注意Survivor区有两个。如3表示Eden：3 Survivor：2，一个Survivor区占整个年轻代的1/5</li>\n<li><code>-XX:MaxPermSize=n</code>：设置持久代大小</li>\n</ul>\n<h3 id=\"gc-设置\">GC 设置<a title=\"#gc-设置\" href=\"#gc-设置\"></a></h3>\n<ul>\n<li><code>-XX:+UseSerialGC</code>：设置串行收集器</li>\n<li><code>-XX:+UseParallelGC</code>：设置并行收集器</li>\n<li><code>-XX:+UseParalledlOldGC</code>：设置并行老年代收集器</li>\n<li><code>-XX:+UseConcMarkSweepGC</code>：设置并发收集器</li>\n<li><code>-XX:+UseG1GC</code></li>\n</ul>\n<h3 id=\"并行收集器设置\">并行收集器设置<a title=\"#并行收集器设置\" href=\"#并行收集器设置\"></a></h3>\n<ul>\n<li><code>-XX:ParallelGCThreads=n</code>：设置并行收集器收集时使用的CPU数，并行收集线程数</li>\n<li><code>-XX:MaxGCPauseMillis=n</code>：设置并行收集最大的暂停时间（如果到这个时间了，垃圾回收期依然没有回收完，也会停止回收）</li>\n<li><code>-XX:+GCTimeRatio=n</code>：设置垃圾回收时间占程序运行时间的百分比。公式为：1/(1+n)</li>\n<li><code>-XX:+CMSIncrementalMode</code>：设置为增量模式。适用于单CPU情况</li>\n<li><code>-XX:ParallelGCThreads=n</code>：设置并发收集器年轻代收集方式为并行收集时，使用CPU数，并行收集线程数</li>\n</ul>\n<h3 id=\"打印gc回收的过程日志信息\">打印GC回收的过程日志信息<a title=\"#打印gc回收的过程日志信息\" href=\"#打印gc回收的过程日志信息\"></a></h3>\n<ul>\n<li><code>-XX:+printGC</code></li>\n<li><code>-XX:+PrintGCDetails</code></li>\n<li><code>-XX:+PrintGCTimeStamps</code></li>\n<li><code>-Xloggc:filename</code></li>\n</ul>\n<hr>\n<h2 id=\"十五、线上问题排查\">十五、线上问题排查<a title=\"#十五、线上问题排查\" href=\"#十五、线上问题排查\"></a></h2>\n<h3 id=\"线上服务cpu占用过高怎么排查？\">线上服务CPU占用过高怎么排查？<a title=\"#线上服务cpu占用过高怎么排查？\" href=\"#线上服务cpu占用过高怎么排查？\"></a></h3>\n<p><strong>问题分析：</strong> CPU高一定是某个程序长期占用了CPU资源</p>\n<ol>\n<li><code>top</code> —— 通过top命令可以找到CPU占用最高的程序进程PID</li>\n<li><code>top -H -p 进程PID</code> —— 可以找到CPU占用最高的线程PID</li>\n<li><code>printf '0x%x\\n' 线程PID</code> —— 可以将该线程PID转换为16进制</li>\n<li><code>jstack 进程PID | grep 16进制线程PID -A 20</code> —— 可以显示线程的状态，是否守护线程，线程的内存地址等信息，也可以看到时哪一行代码。20表示显示20行</li>\n</ol>\n<h3 id=\"内存飙高问题怎么排查？\">内存飙高问题怎么排查？<a title=\"#内存飙高问题怎么排查？\" href=\"#内存飙高问题怎么排查？\"></a></h3>\n<p><strong>问题分析：</strong> 内存飙高如果发生在java进程上，一般是因为创建了大量的对象导致的，持续飙高说明垃圾回收跟不上对象创建的速度，或者内存泄漏导致对象无法回收。</p>\n<p>先使用命令 <code>top</code> 然后按 <code>m</code>，就是按照内存使用量排序，然后就能看到内存占用很大的java进程PID</p>\n<p><strong>1. 先观察垃圾回收的情况</strong></p>\n<ul>\n<li><code>jstat -gc PID 1000</code> —— 查看GC次数，使劲按等信息，每隔一秒打印一次，1000ms输出一次，一直输出</li>\n<li><code>jmap -histo PID | head -20</code> —— 查看堆内存占用空间最大的前20个对象类型，可初步查看时哪个对象占用了内存。</li>\n</ul>\n<p>如果每次GC次数频繁，而且每次回收的内存空间也正常，那说明是因为对象创建速度过快导致内存一直占用很高；如果每次回收的内存非常少，那么很可能是因为内存泄漏导致内存一直无法被回收。</p>\n<p><strong>2. 导出堆内存文件快照</strong></p>\n<figure class=\"highlight bash\"><table><tr><td class=\"gutter\"><pre><span class=\"line\">1</span><br></pre></td><td class=\"code\"><pre><span class=\"line\">jmap -dump:live,format=b,file=/home/myheaddump.hprof PID</span><br></pre></td></tr></table></figure>\n<p>排查哪些数据类型占用很大无法回收：如果通过上面方法，通过占用最大的对象类型无法判断出内存飙高的原因，则通过jmap生成dump文件，然后装入Visualvm分析，在实例数页签下可以逐个查看对象引用外层。</p>\n<h3 id=\"频繁minor-gc怎么办？\">频繁minor gc怎么办？<a title=\"#频繁minor-gc怎么办？\" href=\"#频繁minor-gc怎么办？\"></a></h3>\n<p>首先通过指令 <code>jstat -gc PID 1000</code> 一秒打印一次，主要查看minor gc的耗时和次数，如果确实隔一小会就minor gc，那就可以结合full gc来看，如果full gc能够回收大部分的空间，那就证明可能是新生代空间较小，Eden区很快被填满，导致频繁minor gc，因此可以通过增大新生代空间 <code>-Xmn</code> 来降低Minor gc的频率。</p>\n<h3 id=\"频繁full-gc怎么办？\">频繁full gc怎么办？<a title=\"#频繁full-gc怎么办？\" href=\"#频繁full-gc怎么办？\"></a></h3>\n<p><strong>1. 清楚从程序角度，有哪些原因导致full gc</strong></p>\n<ul>\n<li><strong>大对象：</strong> 系统一次性加载了过多数据到内存中，（比如SQL查询未作分页），导致大对象进入了老年代。</li>\n<li><strong>内存泄漏：</strong> 频繁创建大连对象，但是无法被回收（比如IO对象使用完后未调用close方法释放资源），现引发full gc，最后导致OOM。</li>\n<li><strong>程序频繁生成一些长生命周期的对象，</strong> 当这些对象的存活年龄超过分代年龄时，便会进入老年代，最后引发full gc。</li>\n<li><strong>程序BUG</strong></li>\n<li><strong>代码显式调用gc方法，</strong> 包括自己的代码甚至框架中的代码。</li>\n<li><strong>JVM参数设置问题：</strong> 包括内存大小，新生代和老年代的大小，Eden区和S区的大小，元空间大小，垃圾回收算法等等。</li>\n</ul>\n<p><strong>2. 清楚排查问题时能使用哪些工具</strong></p>\n<ul>\n<li>\n<p>公司的监控系统：大部分公司都有，可全方位监控JVM的各项指标。</p>\n</li>\n<li>\n<p>JDK的自带工具，包括jmap，jstat等常用命令：</p>\n<figure class=\"highlight bash\"><table><tr><td class=\"gutter\"><pre><span class=\"line\">1</span><br><span class=\"line\">2</span><br><span class=\"line\">3</span><br><span class=\"line\">4</span><br><span class=\"line\">5</span><br><span class=\"line\">6</span><br></pre></td><td class=\"code\"><pre><span class=\"line\"><span class=\"comment\"># 查看堆内存各区域的使用率以及GC情况</span></span><br><span class=\"line\">jstat -gcutil -h20 pid 1000</span><br><span class=\"line\"><span class=\"comment\"># 查看堆内存中的存活对象，并按空间排序</span></span><br><span class=\"line\">jmap -histo pid | <span class=\"built_in\">head</span> -n20</span><br><span class=\"line\"><span class=\"comment\"># dump堆内存文件</span></span><br><span class=\"line\">jmap -dump:format=b,file=heap pid</span><br></pre></td></tr></table></figure>\n</li>\n<li>\n<p>可视化的堆内存分析工具：JVisualVM，MAT等</p>\n</li>\n</ul>\n<p><strong>3. 排查指南</strong></p>\n<ul>\n<li>查看监控，以了解出现问题的时间点以及当前full gc的频率（可对比正常情况看频率是否正常）</li>\n<li>了解该时间点之前有没有程序上线，基础组件升级等情况。</li>\n<li>了解JVM参数设置，包括：堆空间各个区域的大小设置，新生代和老年代分别采用了哪些垃圾回收器，然后分析JVM参数设置是否合理。</li>\n<li>在对步骤1中列出的原因做排除，其实元空间被打满，内存泄漏，代码显式调用gc方法比较容易排查。</li>\n<li>针对大对象或者长生命周期对象导致的full gc，可通过jmap -histo命令结合dump堆内存文件做进一步分析，需要先定位到可疑对象。</li>\n<li>通过可疑对象定位到具体代码再次分析，这时候要结合GC原理和JVM参数设置，弄清楚可疑对象是否满足了进入老年代的条件才能下结论。</li>\n</ul>\n<h3 id=\"如何定位线上oom？\">如何定位线上OOM？<a title=\"#如何定位线上oom？\" href=\"#如何定位线上oom？\"></a></h3>\n<p><strong>1. 造成OOM的原因</strong></p>\n<ul>\n<li><strong>一次性申请的太多</strong> → 更改申请对象数量</li>\n<li><strong>内存资源耗尽未释放</strong> → 找到未释放的对象进行释放，例如IO，数据库连接，或者使用连接池</li>\n<li><strong>本身资源不够</strong> → <code>jmap -heap</code> 查看堆信息</li>\n</ul>\n<p><strong>2. 如何快速定位OOM</strong></p>\n<ul>\n<li><strong>系统已经OOM挂了：</strong> 提前设置 <code>-XX:+HeapDumpOnOutOfMemoryError -XX:HeapDumpPath=xxxxx</code></li>\n<li><strong>系统运行中还未OOM：</strong>\n<ul>\n<li>导出dump文件：<code>jmap -dump:format=b,file=test.hprof 14600</code></li>\n<li>Arthas</li>\n</ul>\n</li>\n<li><strong>结合jvisualvm进行调试：</strong> 查看最多跟业务有关对象 → 找到GCRoot → 查看线程栈</li>\n</ul>\n","next":{"title":"查找rabbitMQ消费者服务","link":"2026/04/24/查找rabbitMQ消费者服务"},"plink":"https://pairs-vip.github.io/2026/08/13/JVM虚拟机/","toc":[{"id":"jvm-虚拟机","title":"JVM 虚拟机","index":"1","children":[{"id":"一、jvm-架构概览","title":"一、JVM 架构概览","index":"1.1"},{"id":"二、gc-内存回收流程","title":"二、GC 内存回收流程","index":"1.2"},{"id":"三、gc-安全点","title":"三、GC 安全点","index":"1.3"},{"id":"四、类的生命周期","title":"四、类的生命周期","index":"1.4"},{"id":"五、垃圾收集算法","title":"五、垃圾收集算法","index":"1.5"},{"id":"六、三色标记算法","title":"六、三色标记算法","index":"1.6","children":[{"id":"什么是三色标记","title":"什么是三色标记","index":"1.6.1"},{"id":"对象漏标问题","title":"对象漏标问题","index":"1.6.2"}]},{"id":"七、cms-收集器","title":"七、CMS 收集器","index":"1.7"},{"id":"八、g1-收集器","title":"八、G1 收集器","index":"1.8","children":[{"id":"有了cms，为什么还有引用g1","title":"有了CMS，为什么还有引用G1","index":"1.8.1"}]},{"id":"九、逃逸分析","title":"九、逃逸分析","index":"1.9"},{"id":"十、类加载器","title":"十、类加载器","index":"1.10","children":[{"id":"类加载流程-——-jdk-9-前的双亲委派模式","title":"类加载流程 —— JDK 9 前的双亲委派模式","index":"1.10.1"}]},{"id":"十一、内存泄漏","title":"十一、内存泄漏","index":"1.11"},{"id":"十二、gc-roots","title":"十二、GC Roots","index":"1.12"},{"id":"十三、jvm-监控和故障处理工具","title":"十三、JVM 监控和故障处理工具","index":"1.13","children":[{"id":"jconsole","title":"JConsole","index":"1.13.1"},{"id":"visualvm（jvisualvm）","title":"VisualVM（jvisualvm）","index":"1.13.2"},{"id":"jps","title":"jps","index":"1.13.3"},{"id":"jstat","title":"jstat","index":"1.13.4"},{"id":"jmap","title":"jmap","index":"1.13.5"},{"id":"jhat","title":"jhat","index":"1.13.6"},{"id":"jstack","title":"jstack","index":"1.13.7"},{"id":"arthas","title":"Arthas","index":"1.13.8"}]},{"id":"十四、jvm-常见参数配置","title":"十四、JVM 常见参数配置","index":"1.14","children":[{"id":"堆配置","title":"堆配置","index":"1.14.1"},{"id":"gc-设置","title":"GC 设置","index":"1.14.2"},{"id":"并行收集器设置","title":"并行收集器设置","index":"1.14.3"},{"id":"打印gc回收的过程日志信息","title":"打印GC回收的过程日志信息","index":"1.14.4"}]},{"id":"十五、线上问题排查","title":"十五、线上问题排查","index":"1.15","children":[{"id":"线上服务cpu占用过高怎么排查？","title":"线上服务CPU占用过高怎么排查？","index":"1.15.1"},{"id":"内存飙高问题怎么排查？","title":"内存飙高问题怎么排查？","index":"1.15.2"},{"id":"频繁minor-gc怎么办？","title":"频繁minor gc怎么办？","index":"1.15.3"},{"id":"频繁full-gc怎么办？","title":"频繁full gc怎么办？","index":"1.15.4"},{"id":"如何定位线上oom？","title":"如何定位线上OOM？","index":"1.15.5"}]}]}],"reading_time":"5398 字约 36 分钟"}